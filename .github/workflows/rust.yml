name: Finalize Immutable Successor API Once

on:
  push:
    branches: ["phase-3/directory-snapshots-recovery"]
    paths:
      - ".github/workflows/rust.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase-3/directory-snapshots-recovery
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Normalize and verify successor API
        run: |
          python3 tools/prepare_immutable_successor_source.py
          cargo fmt --all
          cargo fmt --all -- --check
          cargo clippy --locked -p ucof-experiments --test exp0002_immutable_vector --test immutable_successor_api -- -D warnings
          cargo test --locked -p ucof-experiments --test exp0002_immutable_vector
          cargo test --locked -p ucof-experiments --test immutable_successor_api
          cargo test --locked -p ucof-experiments --test exp0002_immutable_generated_vectors
      - name: Restore permanent read-only workflows and commit
        run: |
          rm -f tools/prepare_immutable_successor_source.py
          rm -f .github/workflows/export-formatted-immutable-successor.yml
          python3 - <<'PY'
          from pathlib import Path

          Path('.github/workflows/verify-exp0002-immutable-vector.yml').write_text('''name: Verify Immutable Successor Vector

on:
  pull_request:
    paths:
      - "tests/vectors/exp-0002-immutable/**"
      - "crates/ucof-experiments/src/immutable_successor.rs"
      - "crates/ucof-experiments/src/lib.rs"
      - "crates/ucof-experiments/tests/immutable_successor_api.rs"
      - "crates/ucof-experiments/tests/exp0002_immutable_vector.rs"
      - "crates/ucof-experiments/tests/exp0002_immutable_generated_vectors.rs"
      - ".github/workflows/verify-exp0002-immutable-vector.yml"
  push:
    branches: ["phase-3/**"]
    paths:
      - "tests/vectors/exp-0002-immutable/**"
      - "crates/ucof-experiments/src/immutable_successor.rs"
      - "crates/ucof-experiments/src/lib.rs"
      - "crates/ucof-experiments/tests/immutable_successor_api.rs"
      - "crates/ucof-experiments/tests/exp0002_immutable_vector.rs"
      - "crates/ucof-experiments/tests/exp0002_immutable_generated_vectors.rs"
      - ".github/workflows/verify-exp0002-immutable-vector.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Lint successor parser and reusable API
        run: cargo clippy --locked -p ucof-experiments --test exp0002_immutable_vector --test immutable_successor_api -- -D warnings
      - name: Parse, generate, and validate successor vectors
        run: |
          cargo test --locked -p ucof-experiments --test exp0002_immutable_vector -- --nocapture
          cargo test --locked -p ucof-experiments --test immutable_successor_api -- --nocapture
          cargo test --locked -p ucof-experiments --test exp0002_immutable_generated_vectors -- --nocapture
''', encoding='utf-8')

          Path('.github/workflows/rust.yml').write_text('''name: Rust

on:
  push:
    branches: [main, "phase-1/**", "phase-2/**", "phase-3/**"]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Check locked dependency graph
        run: cargo metadata --locked --no-deps --format-version 1 > /dev/null
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Lint
        run: cargo clippy --locked --workspace --all-targets -- -D warnings
      - name: Test Rust implementation
        run: cargo test --locked --workspace --all-targets
      - name: Test Rust documentation
        run: cargo test --locked --workspace --doc
      - name: Test independent Python parser
        run: python3 tools/validate_exp_0001.py --vectors tests/vectors/exp-0001
      - name: Test adversarial Python cases
        run: python3 tools/test_exp_0001_adversarial.py
      - name: Test independent Phase 3 models
        run: python3 tools/validate_phase3_models.py
      - name: Test independent EXP-0002 codec
        run: |
          python3 tools/exp0002_codec.py --self-test
          python3 tools/exp0002_codec.py --verify-vectors tests/vectors/exp-0002
      - name: Test EXP-0002 adversarial corpus
        run: python3 tools/test_exp0002_adversarial.py
      - name: Verify pinned EXP-0002 invalid corpus
        run: python3 tools/exp0002_invalid_vectors.py --verify-vectors tests/vectors/exp-0002-invalid
      - name: Verify immutable successor invalid recipes
        run: python3 tools/verify_exp0002_immutable_invalid_recipes.py
      - name: Regenerate immutable successor vector recipes
        run: python3 tools/verify_exp0002_immutable_generated_recipes.py
      - name: Reproduce framing experiments
        run: |
          python3 tools/experiment_framing_widths.py
          python3 tools/experiment_footer_discovery.py
          python3 tools/experiment_scale_limits.py
          python3 tools/experiment_directory_models.py
          python3 tools/experiment_exp0002_page_sizes.py
          python3 tools/experiment_exp0002_cow_reuse.py
          python3 tools/experiment_exp0002_checkpoint_cadence.py

  msrv:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.85.0
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: msrv-1.85.0
      - name: Check MSRV
        run: cargo check --locked --workspace --all-targets

  portability:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - i686-unknown-linux-gnu
          - powerpc64-unknown-linux-gnu
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: ${{ matrix.target }}
      - name: Install 32-bit linker support
        if: matrix.target == 'i686-unknown-linux-gnu'
        run: sudo apt-get update && sudo apt-get install -y gcc-multilib
      - name: Check portability target
        run: cargo check --locked --workspace --all-targets --target ${{ matrix.target }}
''', encoding='utf-8')
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/ucof-experiments/src/immutable_successor.rs \
            crates/ucof-experiments/src/lib.rs \
            crates/ucof-experiments/tests/immutable_successor_api.rs \
            tools/prepare_immutable_successor_source.py \
            .github/workflows/export-formatted-immutable-successor.yml \
            .github/workflows/verify-exp0002-immutable-vector.yml \
            .github/workflows/rust.yml
          git diff --cached --check
          git commit -m "feat: land reusable immutable successor API"
          git push origin HEAD:phase-3/directory-snapshots-recovery
